<main>
    <h2>HOME</h2>
    {{#each books}}
        <div class="books">
            <h3>Titulo do livro: {{this.title}}</h3>
            <p>Quantidade de paginas {{this.pageqty}}</p>
        </div>
    {{/each}}
</main>